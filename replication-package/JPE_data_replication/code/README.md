# Code Folder

Place all source code needed to reproduce every computational result in the paper and appendices.

Current layout:

- `code/run_all.m`: top-level driver script
- `code/Figure1.m`: generates Figure 1 panel outputs
- `code/Figure2.m`: generates Figure 2 panel outputs

Requirements:

- Use relative paths.
- Avoid manual point-and-click steps.
- Save outputs to `output/`.
- Document package/library dependencies in root `README.md`.
